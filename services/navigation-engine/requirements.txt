# ─────────────────────────────────────────────────────────────
# Web framework + server
# ─────────────────────────────────────────────────────────────
fastapi==0.115.5          # HTTP framework — defines all API routes
uvicorn[standard]==0.32.1 # ASGI server — runs FastAPI in production and dev

# ─────────────────────────────────────────────────────────────
# Data validation + config
# ─────────────────────────────────────────────────────────────
pydantic==2.10.3          # Data models and validation (must be v2)
pydantic-settings==2.6.1  # Reads .env file into Settings class
python-multipart==0.0.18  # Parses file uploads (floor plan PNG, form images)

# ─────────────────────────────────────────────────────────────
# Routing / graph algorithms
# ─────────────────────────────────────────────────────────────
networkx==3.4.2           # Graph data structure + Dijkstra shortest path

# ─────────────────────────────────────────────────────────────
# Map digitization / computer vision
# ─────────────────────────────────────────────────────────────
opencv-python-headless==4.10.0.84  # Image processing (walkable mask detection)
scikit-image==0.26.0               # Skeletonization — thins corridors to 1px lines
numpy==2.1.3                       # Numeric arrays used by OpenCV and scikit-image
Pillow==11.0.0                     # Load and save PNG/JPG floor plan images

# ─────────────────────────────────────────────────────────────
# Testing
# ─────────────────────────────────────────────────────────────
pytest==8.3.3             # Test runner
pytest-asyncio==0.24.0    # Support for async test functions
httpx==0.28.0             # HTTP client for testing FastAPI endpoints
